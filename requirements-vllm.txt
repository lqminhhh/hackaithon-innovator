# Entropy-Gated Jury — Linux/CUDA requirements (vLLM backend)
# Use on any Linux machine with CUDA 12.x and Python 3.10+
#
# Quick start (bare Linux):
#   pip install -r requirements-vllm.txt
#
# Quick start (Colab A100):
#   !pip install -q -r requirements-vllm.txt

# vLLM ships its own torch build — do NOT pin torch separately
vllm>=0.9.0

# HuggingFace ecosystem
transformers>=4.51.0
huggingface-hub>=0.30.0
tokenizers>=0.21.0
accelerate>=1.3.0

# RAG / retrieval
sentence-transformers>=3.3.1
faiss-cpu>=1.8.0          # use faiss-gpu if FAISS GPU build is available

# General utilities
pandas>=2.2.2
numpy>=1.26.4
tqdm>=4.66.4
pyyaml>=6.0.1
sympy>=1.13.0
