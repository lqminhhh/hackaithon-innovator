torch>=2.6.0,<2.8.0
torchvision>=0.21.0,<0.23.0
# Qwen3.5 requires transformers>=5.2.0 (model_type='qwen3_5' not in older releases).
transformers>=5.2.0,<6.0.0
tokenizers>=0.22.0
bitsandbytes>=0.49.2
pandas>=2.2.2
numpy>=1.26.4
tqdm>=4.66.4
pyyaml>=6.0.1
accelerate>=1.1.1
huggingface-hub>=0.26.0
pytest>=8.3.2,<9.0.0
# vLLM is OPTIONAL — install separately in CUDA environments:
#   pip install "vllm>=0.17.0"
# vLLM>=0.17.0 is required for Qwen3.5 support (qwen3_5 architecture).
# The official Qwen3.5 model card recommends vLLM for deployment.
#
# hf_transfer is OPTIONAL — only needed when HF_HUB_ENABLE_HF_TRANSFER=1
# (some cloud pods, e.g. RunPod, set this by default). Otherwise skip it.
#   pip install hf_transfer
# Or disable fast transfer: HF_HUB_ENABLE_HF_TRANSFER=0
