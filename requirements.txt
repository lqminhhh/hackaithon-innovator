# Core GPU inference stack.
# Notes:
# - vLLM is required for the intended submission path.
# - This repo's Docker image targets NVIDIA CUDA 12.2 per BTC guidance.
# - If vLLM is "unavailable" on a machine, the usual causes are missing package
#   install, unsupported/no NVIDIA GPU, or an incompatible host driver.
torch==2.11.0
torchvision
transformers==5.12.1
tokenizers
bitsandbytes>=0.49.2
pandas==3.0.3
numpy>=1.26.4
PyYAML==6.0.3
accelerate>=1.1.1
huggingface-hub>=0.26.0
matplotlib>=3.8
pytest==8.4.2
vllm==0.23.0
# hf_transfer is OPTIONAL — only needed when HF_HUB_ENABLE_HF_TRANSFER=1
# (some cloud pods, e.g. RunPod, set this by default). Otherwise skip it.
#   pip install hf_transfer
# Or disable fast transfer: HF_HUB_ENABLE_HF_TRANSFER=0
