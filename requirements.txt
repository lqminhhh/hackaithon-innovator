# Entropy-Gated Jury — base requirements (CPU-compatible, no CUDA/vLLM)
# Install on MacBook: pip install -r requirements.txt
# Then for local Metal inference: pip install -r requirements-local.txt

torch>=2.4.0,<2.6.0
torchvision>=0.19.0,<0.21.0
# Pin below 4.48 — newer transformers require torch.float8_e8m0fnu (PyTorch 2.6+)
transformers>=4.46.3,<4.48.0
sentence-transformers>=3.3.1,<4.0.0
faiss-cpu>=1.8.0
pandas>=2.2.2
numpy>=1.26.4
tqdm>=4.66.4
pyyaml>=6.0.1
accelerate>=1.1.1
huggingface-hub>=0.26.0
sympy>=1.13.0

# vLLM is OPTIONAL and installed separately in Docker (pinned main-branch commit).
# Do NOT add vllm here; it upgrades torch/transformers and breaks the stack.
# See Dockerfile for the pinned install command.

# bitsandbytes only needed for HuggingFace 4-bit loading (not used in EGJ):
# bitsandbytes>=0.49.2
