torch>=2.4.0,<2.6.0
torchvision>=0.19.0,<0.21.0
# Pin below 4.48 — newer transformers require torch.float8_e8m0fnu (PyTorch 2.6+)
transformers>=4.46.3,<4.48.0
bitsandbytes>=0.49.2
sentence-transformers>=3.3.1,<4.0.0
faiss-cpu>=1.8.0
rank_bm25==0.2.2
pandas>=2.2.2
numpy>=1.26.4
tqdm>=4.66.4
pyyaml>=6.0.1
accelerate>=1.1.1
huggingface-hub>=0.26.0
pytest>=8.3.2,<9.0.0
# vLLM is OPTIONAL — install separately after base deps (Colab CUDA 12):
#   pip install vllm==0.6.3.post1
# Do NOT add vllm to requirements.txt; it upgrades torch/transformers and breaks the stack.
